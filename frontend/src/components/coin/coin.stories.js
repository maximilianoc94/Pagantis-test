import React from 'react';

import Coin from '.';

export default {
  title: 'Components/Coin Icon',
  component: Coin,
};

const Template = (args) => <Coin {...args} />;

export const Default = Template.bind({});
